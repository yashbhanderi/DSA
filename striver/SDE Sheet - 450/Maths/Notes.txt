- Count digits of a number = floor(log10(num) + 1)

- Convert number to string = to_string(num)

- Modula of negative = negative
    
    (-321)%10 = -1

- Armstrong number formula

    abcd... = pow(a,n) + pow(b,n) + pow(c,n) + pow(d,n) + ....

- 555 % 1000 = 555